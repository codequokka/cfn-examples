name: Run cfn-nag

on:
  push:
    branches:
  pull_request:
    branches:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Run cfn-nag
        uses: minchao/cfn-nag-action@master
        with:
          args: "--input-path ./cfn/templates/"
